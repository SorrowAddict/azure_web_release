{
    "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "react-icons": "^5.0.1",
        "react-router-dom": "^6.22.0",
        "react-scripts": "5.0.1",
        "react-toastify": "^10.0.4"
    },
    "scripts": {
        "test": "cd public && yarn start && cd .. && cd server && npm start",
        "start": "cd public && yarn start && cd .. && cd server && npm start"
    }
}
